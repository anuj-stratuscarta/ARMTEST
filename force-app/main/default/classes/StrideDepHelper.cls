public with sharing class StrideDepHelper {
    // The DEPENDENCY. Seeded into the source org only — a target that lacks it is the whole point.
    public static String greet() {
        return 'hello from StrideDepHelper';
    }
}